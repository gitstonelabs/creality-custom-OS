# Built kernel artifacts
zImage
*.dtb

# Android bootimg + raw kernel images
boot.img
*.img
kernel_stock*.img

# swupdate packages
*.swu

# Built rootfs images
rootfs.squashfs
rootfs.tar

# Buildroot output tree
build-output/

# AIC8800 vendor firmware blobs, excluded on purpose, see NOTICES.md.
rootfs/package/aic8800-firmware/lib/firmware/
